#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - print random number and if positive or negative
 *
 *Description : /
 *Return: 0 if it worked
 */
int main(void)
{
char ch = 122;
char space = '\n';
for (ch = 122; ch >= 97; ch--)
putchar(ch);
putchar(space);
return (0);
}
