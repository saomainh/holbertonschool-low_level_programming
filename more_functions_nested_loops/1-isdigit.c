#include "main.h"

/**
 *_isdigit - it just prints the alphabet
 *@c : eaofhezui
 *Description : /
 *Return: no return
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
