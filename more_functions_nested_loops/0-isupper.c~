#include "main.h"

/**
 *print_alphabet - it just prints the alphabet
 *
 *Description : /
 *Return: no return
 */

int _isupper(int c)
{
	_putchar(c + '0');
	if (c > 65 && c < 90)
	{	
		return (1);
	}	
	else
	{
		return (0);
	}	
}	
