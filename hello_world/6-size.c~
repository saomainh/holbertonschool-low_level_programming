#include <stdio.h>

/**
 * main - prints a quote
 *
 * Description: prints a single line using printf followed by a new line
 * Return: 0 if it works
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
