#include "main.h"

/**
 *print_alphabet_x10 - it just prints the alphabet x10
 *
 *Description : /
 *Return: no return
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
