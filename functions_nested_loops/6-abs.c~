#include "main.h"

/**
 *_abs - return the absolute value of a number
 *@n: number  to test
 *Description : /
 *Return: absolute value of n
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return(n);
	}
	else
	{
		return(n);
	}
}
