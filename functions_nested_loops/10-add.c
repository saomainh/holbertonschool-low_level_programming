#include "main.h"

/**
 *add - bsolute value of a number
 *@a : first number to add
 *@b : second number to add
 *Description : /
 *Return: result
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
