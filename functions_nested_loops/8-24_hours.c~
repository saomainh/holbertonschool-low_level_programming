#include "main.h"

/**
 *print_last_digit - bsolute value of a number
 *@n: number  to test
 *Description : /
 *Return: absolute value of n
 */

void jack_bauer(void)
{
	int i;
	for (i = 0; i <= 24; i++)
	{
		_putchar(i / 10 + '0');
		_putchar(i % 10 + '0');
		_putchar(':');
		_putchar('\n');
	}
}
