#include "main.h"

/**
 *_puts - it just prints the alphabet
 *@str : eaofhezui
 *Description : /
 *Return: no return
 */

void puts2(char *str)
{
	while (*str != '\0')
	{
		if (*str % 2 == 0)
		
	}
	_putchar('\n');
}
