#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *str;
abcdefghijk lmnoprstuvwxyz
    str = "abcdefghijk lmnoprstuvwxyz";
    puts2(str);
    return (0);
}
