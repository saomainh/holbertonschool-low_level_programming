#include "main.h"

/**
 *swap_int - it just prints the alphabet
 *@a : eaofhezui
 *@b : afeaz
 *Description : /
 *Return: no return
 */

int _strlen(char *s)
{
	int i = 0;
	int count = 0;
	
	while (s[i] != s[-1])
	{
		count++;
		i++;
	}

	return (count);
}	

